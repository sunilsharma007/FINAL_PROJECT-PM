import { SignupInfoUser } from './signup-info-user';

describe('SignupInfoUser', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new SignupInfoUser()).toBeTruthy();
  });
});
